#pragma once

const char *WIFI_SSID = "YOUR_WIFI_SSID";
const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// Optional: set a non-empty API key to require ?key=YOUR_KEY on /command and /status.
const char *API_KEY = "";
